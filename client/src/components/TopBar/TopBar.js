import React from "react";
import "./TopBar.css";

const TopBar = (props) => (
  <div className="topbar-wrapper">
    <div className="topbar-title">Mi Meals</div>
    <div className="topbar-leftover">
      <span className="topbar-icon">icon</span>
    </div>
  </div>
);

export default TopBar;
